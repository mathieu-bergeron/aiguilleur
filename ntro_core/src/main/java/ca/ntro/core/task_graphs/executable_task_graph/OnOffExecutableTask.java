package ca.ntro.core.task_graphs.executable_task_graph;

public interface OnOffExecutableTask extends ExecutableTask {
	
	/* This creates ObjectMapReader that always hasResult() 
	 * when the task is On and never hasResult() when the task
	 * if off
	 */

}
