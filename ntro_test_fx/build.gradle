plugins {
    id 'localJavaPlugin'
    id 'localJSweetPlugin'
}

project.group = 'ca.ntro'
project.version = '0.1'


dependencies {
    implementation project(':jj_core')
    implementation project(':jj_core_jdk')

    implementation project(':ntro_core')
    implementation project(':ntro_core_jdk')
    implementation project(':ntro_core_fx')

    testImplementation project(':ntro_test')
    testImplementation project(':ntro_test_jdk')

    implementation 'junit:junit:4.13'
}

configurations{
    // XXX: not needed in runtime 
    //      and conflicts with selenium and gson
    testRuntime.exclude group: 'org.jsweet'
    runtime.exclude group: 'org.jsweet' 
}


